# SSAS Tabular Util

```
  -s, --ssas      Required. SSAS Server

  -a, --action    Required. Action: deploy|export|list|tmsl

  -f, --input     Input list file for database

  -o, --out       Out Directory

  -c, --conn      Deploy Connection

  -r, --roles     Deploy Roles

  -m, --member    Deploy Roles Members

  -x, --script    Use with deploy to generate xmla script into out dir
```